[
  {
    "namespace": "keyboard",
    "description": "Chrome-privileged support for WebKeyboard.",
    "functions": [
      {
        "name": "lock",
        "type": "function",
        "async": true,
        "description": "Set the per-code keyboard-lock filter for a tab.",
        "parameters": [
          {
            "name": "tabId",
            "type": "integer",
            "minimum": 0
          },
          {
            "name": "codes",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        ]
      },
      {
        "name": "unlock",
        "type": "function",
        "async": true,
        "description": "Clear the per-code keyboard-lock filter for a tab.",
        "parameters": [
          {
            "name": "tabId",
            "type": "integer",
            "minimum": 0
          }
        ]
      }
    ]
  }
]
